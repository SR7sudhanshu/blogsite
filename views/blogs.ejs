<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>blog</title>
    <%- include('./partials/head') %>

    <style>
        body {
          background: linear-gradient(135deg, #0f172a, #414346);
        }
  
        .hero h1 {
          font-size: 64px;
          text-transform: uppercase;
          letter-spacing: 5px;
          line-height: 1.2;
          color: #fff;
          margin-bottom: 30px;
        }
  
        .hero p {
          font-size: 18px;
          color: #aaa;
          margin-bottom: 50px;
        }
  
        .btn-custom {
          padding: 15px 30px;
          background: #0d6efd;
          color: #fff;
          border: none;
          font-size: 18px;
          text-transform: uppercase;
          cursor: pointer;
          transition: transform 0.3s ease;
        }
  
        .btn-custom:hover {
          transform: scale(1.1);
          background: #0056b3;
        }
  
        /* 3D Card Effect */
        .card-container {
          display: flex;
          flex-wrap: wrap;
          justify-content: space-around;
          margin-top: 40px;
          gap: 40px;
          background: transparent
        }
  
        .card {
          background: #1e293b;
          padding: 30px;
          border-radius: 15px;
          width: 300px;
          box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
          transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
  
        .card:hover {
          transform: translateY(-10px);
          box-shadow: 0 20px 40px rgba(103, 3, 75, 0.3);
        }
  
        .card img {
          border-radius: 10px;
          max-height: 200px;
          width: 100%;
          object-fit: cover;
          transition: transform 0.3s ease;
        }
  
        .card:hover img {
          transform: scale(1.05);
        }
  
        .card-title {
          font-size: 22px;
          font-weight: 600;
          color: #fff;
          margin-top: 20px;
        }
  
        .card-text {
          color: #ccc;
          font-size: 16px;
          margin-top: 10px;
        }
  
        .card-footer {
          display: flex;
          justify-content: space-between;
          align-items: center;
          margin-top: 20px;
        }
  
        .card-footer .btn {
          background: #0d6efd;
          border: none;
          color: #fff;
          padding: 8px 15px;
          border-radius: 5px;
          font-size: 14px;
          cursor: pointer;
          transition: transform 0.3s;
        }
  
        .card-footer .btn:hover {
          transform: scale(1.1);
        }
      </style>

</head>
<body>
    <%- include('./partials/nav') %>
     <section class="card-container">
      <% blogs.forEach(blog => { %>
        <div class="card">
          <img src="<%= blog.coverImageURL %>" alt="Blog Image">
          <div class="card-body">
            <h5 class="card-title"><%= blog.title %></h5>
            <p class="card-text"><%= blog.body.substring(0, 150) %>...</p>
            <div class="card-footer">
              <span>Posted by <b><%= blog.createdBy.fullname %></b></span>
              <a href="/dumps/blogs/<%= blog._id %>" class="btn">Read More</a>
            </div>
          </div>
        </div>
      <% }) %>
    </section>

    <%- include('./partials/script') %>
</body>
</html>